package ehupatras.webrecommendation.preprocess;

public class Sessioning {
	
	// We suppose after this time that a new session starts.
	// We split up sequences in two sessions when we see this jump of time between clicks.
	private int m_maxminutes = 10;
	
}
